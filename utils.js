// module.exports = class Utils {
//   static getAlbumsFromSeries(localSeries) {
//     const localAlbums = {};
//     Object.keys(localSeries).forEach((serieId) => {
//       const serie = localSeries[serieId];
//       Object.keys(serie.albums).forEach((albumId) => {
//         const album = serie.albums[albumId];
//         localAlbums[albumId] = album;
//       });
//     });
//     return localAlbums;
//   }
// };

